---
title: Pawel Cebula - Contacts
heroHeading: Contact me
heroSub: You can find my work online and reach our through these channels
---
{% extends 'base.njk' %}

{% block main %}
  <div class="wrapper">
    {# <ul class="list-reset contacts">
      {% for contact in contacts %}
        <li>
          <a href="{{ contact.url }}">
            {% include 'svg/' + contact.icon + '.svg' %}
            {{ contact.prompt }}
          </a>
        </li>
      {% endfor %}
    </ul> #}
    <div class="contacts">
      {% for contact in contacts %}
        <div class="contact">
          <a href="{{ contact.url }}">
            {% include 'svg/' + contact.icon + '.svg' %}
            <span class="contact__prompt">{{ contact.prompt }}</span>
          </a>
        </div>
      {% endfor %}
    </div>
  </div>
{% endblock main %}