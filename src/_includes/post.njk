{% extends 'base.njk' %}

{% set heroHeading = title %}
{% set heroComment = 'Published: ' + date | dateFilter + ' | ' + content | readingTime + ' read' %}

{% block main %}
  <div class="wrapper post">
    <article 
      class="post__content"
      aria-labelledby="heroHeading"
      aria-describedby="heroSub heroComment"
    >
      {{ content | safe }}
    </article>
  </div>
{% endblock main %}